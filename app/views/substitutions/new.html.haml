%p
  Substitution count:
  = @count

%ul
  - @substitutions.each do |substitution|
    %li= substitution

%h2 New substitution

= form_for @substitution do |f|
  = f.text_field :input,  placeholder: 'Input'
  = f.text_field :output, placeholder: 'Output'

  = f.submit 'Save'
